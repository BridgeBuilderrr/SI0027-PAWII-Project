# Mini Project - Angular Standalone

Lihat README utama pada folder [`pertemuan-21-22-form-validasi-spa`](../README.md) untuk penjelasan lengkap form & validasi, cara menjalankan, dan kaitannya dengan Tugas Project Mandiri.

Lengkapi setiap `// TODO` pada `src/app/components/mahasiswa-form/mahasiswa-form.component.ts` (`Validators` & `onSubmit()`), lalu jalankan:

```bash
npm install
npm start
```
